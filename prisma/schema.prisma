generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model SensorData {
  id     String   @id @default(auto()) @map("_id") @db.ObjectId
  uid    String
  uxt    Int
  readings Json
}